services:
    blazorapp:
        image: aliasvault
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - "80:8080"
        volumes:
            - ./database:/database
        restart: always
